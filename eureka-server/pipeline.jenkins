println readFileFromWorkspace("${WORKSPACE}/cicd/jobdsl/pipelineJob.groovy")

def pipelineJob = evaluate(readFileFromWorkspace("${WORKSPACE}/cicd/jobdsl/pipelineJob.groovy"))

println pipelineJob

pipelineJob.definePipelineJob()
